# ==============================================================================
# Environment Configuration - Physical State Field Substrate
# ==============================================================================

# OpenAI / ChatGPT API Key: Used by the Physical Query Engine to verbalize & explain
# structured, deterministic quantum-embedding substrate queries (e.g., gpt-4o, gpt-4o-mini).
# Note: The Substrate runs 100% deterministically offline even without an API key!
OPENAI_API_KEY=""

# xAI Grok API Key: Optional secondary provider for physical state interpretation.
GROK_API_KEY=""

# Application Port (default: 3000)
PORT=3000
